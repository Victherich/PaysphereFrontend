This project is built with create react app
user login and token details are being held with redux persist to keep the user sign in untill he chooses to sign out.
React icons are used for the icons, sweetalert for loading states and success and failure messages, 
The styles are built with styled components.
you can use the normal git commads to clone the project and it will run well on your local host.
